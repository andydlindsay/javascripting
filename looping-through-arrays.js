// variable declaration
var pets = ['cat', 'dog', 'rat'];

// for loop to add 's' to each element in pets
for (var i = 0; i < pets.length; i++) {
  pets[i] = pets[i] + 's';
}

// output pets array to the console
console.log(pets);
